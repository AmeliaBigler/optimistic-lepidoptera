{{#each articles as |article| }}
<div class="row">
  <div class="col">
    <a href="/article/{{article.id}}">{{article.title}}</a>
    <h3>{{format_date article.date_created}}</h3>
  </div>
</div>
{{/each}}

{{!-- add article publish date --}}